<h1><strong>Starshop</strong>: you are not logged in</h1>

<div>

    Check {{ numberOfStarshipCount }} <br>
    Check {{ 'numberOfStarshipCount' }} <br>
    Check {{ numberOfStarshipCount * 10 }} <br>

    {% if numberOfStarshipCount > 300 %}
        <p>
            {# my comment #}
            My "do statement" is working
        </p>
    {% endif %}
</div>
<div>
    <h2> My Ship Info </h2>
    <table>
        <tr>
            <th>Name</th>
            <td>{{ myShip.name }}</td>
        </tr>
        <tr>
            <th>Class</th>
            <td>{{ myShip.class }}</td>
        </tr>
        <tr>
            <th>Captain</th>
            <td>{{ myShip.captain|upper|lower|title }}</td>
        </tr>
        <tr>
            <th>Status</th>
            <td>{{ myShip.status }}</td>
        </tr>
        </tr>
    </table>
</div>
